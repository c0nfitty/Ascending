[project]
name = "maplerugs"
version = "0.1.0"
description = "Maple Rugs AI description and semantic search"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.35.0",
    "fastapi>=0.136.1",
    "pydantic-settings>=2.14.0",
    "strands-agents>=0.1.0",
    "uvicorn[standard]>=0.46.0",
]

[tool.poe.tasks]
start = "python agent.py"
lint = "ruff check ."
format = "ruff format ."

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/maplerugs"]

[dependency-groups]
dev = [
    "poethepoet>=0.44.0",
    "ruff>=0.9.0",
]

[tool.ruff]
line-length = 120
target-version = "py311"
exclude = ["bestsofar3.py"]                                                                                               

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
"src/maplerugs/agents/description_agent.py" = ["E501"]
